import * as React from 'react';
import * as ReactDOM from 'react-dom';
import App, {AppProps} from './components/App.tsx'

ReactDOM.render(
  <App name="apoorv" title="dkv">
    middle
  </App>,
  document.getElementById('app') as HTMLElement
);
